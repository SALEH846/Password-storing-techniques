create table auth1 (
	username text,
	password text
);

create table auth2 (
	username text,
	password text
);

create table auth3 (
	username text,
	password text,
	salt text
);

create table auth4 (
	username text,
	password text
);

create table auth5 (
	username text,
	userdata text
);